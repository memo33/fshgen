#!/bin/sh
# Invocation of fshgen command-line interface on Linux or macOS.
# In a terminal, call:
#
#     ./fshgen --help

SCRIPTDIR="${0%/*}"

java -jar -Dfile.encoding=UTF-8 -Djava.awt.headless=true "$SCRIPTDIR/fshgen.jar" "$@"
